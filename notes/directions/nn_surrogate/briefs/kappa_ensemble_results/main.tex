%=======================================================================
%  BRIEF: results of the vertical-mixing perturbation ensemble, and the
%  recommendation on vertical diffusivity as a surrogate input.
%
%  MAIN DOCUMENT -- structure only; every word of prose lives in sections/.
%  Compile this file. If this brief is its own Overleaf project, Overleaf selects
%  it automatically as the only file carrying \documentclass; if the whole
%  notes/directions/nn_surrogate/ folder was uploaded as one project there are four such
%  files, and you pick between them with the gear icon at the bottom-left corner
%  of the editor -> Compiler -> main document. Not Menu -> Settings.
%
%  SCOPE: the executed ensemble's results (master Part I, Results section) and
%  what they mean for the surrogate's inputs and training data (master Part II).
%  It is the reporting companion to ../perturbation_experiment/, which was the
%  proposal; this is what came back.
%
%  DERIVED FROM ../../master_plan/, which is authoritative -- specifically its
%  Part I Results section (sections/11_part1_results.tex), whose numbers all
%  come from the executed analysis suite at
%  analyses/DINO_1deg/adjoint/kappa_v_ensemble/. A correction that
%  arrives on this brief belongs in the master first.
%
%  TWO CAPS, both deliberate. FOUR PAGES MAXIMUM -- one more than the usual
%  brief cap, granted when this brief was commissioned because it carries two
%  result figures -- and AT MOST THREE FILES under sections/; the title block
%  rides at the top of 01_. A brief that outgrows either has stopped being a
%  brief: cut it, or let the master carry the detail.
%
%  Deliberately standalone -- own preamble, shares no file with the master or
%  the other briefs -- so it can be its own Overleaf project and be shared on
%  its own. Self-contained: pdfLaTeX, the two PNGs under figures/ travel with
%  the folder. Four interactive animations accompany this brief as separate
%  HTML files (listed in sections/02_); they cannot ride inside a PDF.
%
%  No reader is named anywhere in this document.

%  INPUT PATH. Overleaf compiles from the PROJECT ROOT, not from the folder the
%  main file sits in. The \input@path below lists this document's own folder
%  first and the compilation directory second, so the same source builds whether
%  the project root is this folder (one Overleaf project per document, and the
%  local `cd <doc> && latexmk` habit) or notes/directions/nn_surrogate/ (the whole direction
%  dropped in as one project, four documents switched between with the gear
%  icon -> Compiler). Do not remove it: without it the whole-folder upload
%  fails on the very first \input. \graphicspath repeats the same trick for
%  the two figures.
%=======================================================================
\documentclass[10pt,a4paper]{article}

\makeatletter
\expandafter\edef\csname input@path\endcsname{{briefs/kappa_ensemble_results/}{./}}
\makeatother

\input{preamble}
\graphicspath{{briefs/kappa_ensemble_results/figures/}{figures/}}

\begin{document}

\input{sections/01_question_and_answer}
\input{sections/02_findings}
\input{sections/03_surrogate_recommendation}

\end{document}
